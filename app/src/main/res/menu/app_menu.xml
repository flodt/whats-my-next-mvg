<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
            android:id="@+id/refresh_button"
            android:title="@string/refresh"
            app:showAsAction="always"
            android:icon="@drawable/ic_refresh"
    />
    <item
            android:id="@+id/switch_list_button"
            android:title="@string/switch_view_button"
            app:showAsAction="always"
            android:icon="@drawable/ic_list_switch"
    />
    <item
            android:id="@+id/select_station_button"
            android:title="@string/select_station_title"
            app:showAsAction="ifRoom"
            android:icon="@drawable/ic_station_selection"
    />
    <item
        android:id="@+id/exclude_button"
        android:title="@string/exclude"
        app:showAsAction="ifRoom"
        android:icon="@drawable/ic_excluded"
    />
</menu>