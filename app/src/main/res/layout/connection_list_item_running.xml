<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
                                                   android:layout_width="match_parent"
                                                   xmlns:app="http://schemas.android.com/apk/res-auto"
                                                   android:background="@color/white"
                                                   android:layout_height="wrap_content">


    <View
            android:id="@+id/running_bar"
            android:layout_width="10dp"
            android:layout_height="20dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:layout_marginStart="65dp"
            android:background="@color/mvg_1"
    />

    <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/running_line_info"
            app:layout_constraintLeft_toRightOf="@id/running_bar"
            app:layout_constraintTop_toTopOf="@id/running_bar"
            app:layout_constraintBottom_toBottomOf="@id/running_bar"
            android:layout_marginStart="40dp"
            android:text="(3 stops)"
            android:textStyle="italic"
            android:textSize="12sp"
            android:textColor="@color/black"
    />
</androidx.constraintlayout.widget.ConstraintLayout>